services:
  yolov8:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    restart: unless-stopped
    # Ressourcen-Limits (CPU-only)
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 2G